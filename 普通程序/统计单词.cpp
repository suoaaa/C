#include<stdio.h>
int main()
{
	int i=0,j=0;
	char word[256];
	gets(word);
	for(;word[i]==' ';i++)
	{
	}
	while(word[i]!='\0')
	{
		for(;word[i]!=' '&&word[i]!='\0';i++)
		{
		
		}
		for(;word[i]==' '&&word[i]!='\0';i++)
		{
		
		}
		j++;
	}

	printf("%d",j);
	return 0;
 } 
